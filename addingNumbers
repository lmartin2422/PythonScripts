"""
https://edabit.com/challenge/2RtztnzMDdyAj2MD3

Create a function that takes two number strings and
returns their sum as a string.
"""


def add(n1, n2):
    if n1 == "" or n1 is None:
        return "Invalid Operation"
    elif n2 == "" or n2 is None:
        return "Invalid Operation"
    else:
        result = int(n1) + int(n2)
        return str(result)


print(add("111", "111"))
print(add("10", "80"))
print(add("", "20"))










# # Check if either input is an empty string or None
# if not num1 or not num2:
#     return "Invalid Operation"
#
# # Convert the number strings to integers, add them, and return the sum as a string
# return str(int(num1) + int(num2))
